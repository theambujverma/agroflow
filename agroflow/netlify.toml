[build]
  # Publish directory for Netlify. Use '.' when site files are at repository root.
  publish = "."

# If you have a build step (e.g., for frameworks) set `command` accordingly.
# command = "npm run build"
